apiVersion: v1
kind: Pod
metadata:
  name: moisture
  labels:
    app: moisture
spec:
  containers:
    - name: moisture-container
      image: p2w2/moisture
      volumeMounts:
        - name: tz-config
          mountPath: /etc/localtime
      env:
        - name: SERVER_HOST
          value: "${brokerIP}"
        - name: "DEVICE_NAME"
          value: "${nodeName}"
      securityContext:
        privileged: true
  nodeName: ${nodeName}
  volumes:
    - name: tz-config
      hostPath:
        path: /usr/share/zoneinfo/Europe/Berlin

